{{!-- <div class="cube">
    <img class="cube" src="{{imageUrl}}">
    <p class="name">{{name}}</p>
    <p><span>Difficulty level:</span> {{difficultyLevel}}</p>
    <a class="btn" href="/details/{{_id}}">details</a>
    <a class="btn" href="/attach/accessory/{{_id}}">Add</a>
</div> --}}

<div class="col-lg-3 col-md-6 mb-4">
    <div class="card h-100">
        <img class="card-img-top"
            src="{{carImage}}"
            alt="">
        <div class="card-body">
            <h4 class="card-title">{{startPoint}} - {{endPoint}}</h4>
        </div>
        <div class="card-footer">
            <a href="/details/{{_id}}" class="btn btn-primary">Find Out More!</a>
        </div>
    </div>
</div>